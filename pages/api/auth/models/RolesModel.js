// import { Schema, model, models } from 'mongoose'

// const RolesModel = new Schema({
//     name: { type: String, unique: true },
// })

// export default  models.models || model('models', RolesModel)